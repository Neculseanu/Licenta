# Browser Configuration
browserType=CHROME
Base_URL=https://www.amazon.com

# Headless Mode Configuration - Critical for CAPTCHA avoidance
headless.mode=false
headless.window.size=1920,1080

# Timeout Configuration (în secunde)
default.timeout=30
page.load.timeout=60

# Test Configuration  
screenshots.on.failure=true

# Logging Level
logging.level=INFO